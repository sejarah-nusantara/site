#
# This file is automatically generated by the installation process from the file deploy/django_settings.in
# Any edits will be overwritten
#

from project.settings import *

DEBUG = ${debug}

# the public-facing URL of the repository, for example 'http://repository-dasa.anri.go.id'
REPOSITORY_PUBLIC_URL = '${repository_public_url}'

# the 'internal' URL of the repository, for example 'http://localhost:5000'
REPOSITORY_URL = '${repository_url}'

PAGEBROWSER_URL = '${pagebrowser_url}'
PAGEBROWSER_PUBLIC_URL = '${pagebrowser_public_url}'
PAGEBROWSER_USER = '${pagebrowser_user}'
PAGEBROWSER_AUTH = ${pagebrowser_auth}
PAGEBROWSER_ADMIN_URL = 'http://${pagebrowser_user}@${pagebrowser_url}'

DEFAULT_FROM_EMAIL = '${default_from_email}'

GOOGLE_ANALYTICS_ID = '${google_analytics_id}'

${extra_settings}

LOGGING['handlers']['sys_error_log'] = {
    'level': 'ERROR',
    'class': 'logging.handlers.RotatingFileHandler',
    'filename': '/var/log/dasa/website.error.log',
    'maxBytes': 1024 * 1024 * 1,  # 1 MB
    'backupCount': 100,
    'formatter': 'verbose',
}

LOGGING['loggers']['django.request']['handlers'] = LOGGING['loggers']['django.request']['handlers'] + ['sys_error_log']